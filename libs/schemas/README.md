# Shared Schemas

This folder stores canonical JSON schemas shared by the web app, orchestrator, and runner.

## Available schemas

- `agent_payload.schema.json`: typed contract for task handoffs between agents.
